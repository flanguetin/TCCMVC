@model IEnumerable<ASP1_TCC.court>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.courtName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.courtName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idcourt }) |
            @Html.ActionLink("Details", "Details", new { id=item.idcourt }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idcourt })
        </td>
    </tr>
}

</table>
